add docker support for my project.   i need following features:

- images/ folder and cache/ folder need to be mounted as a volume and configurable via env variable
- port need to be configurable via env variable
- images/ folder need to be mounted as read only
- cache/ folder need to be mounted as read write
- run environment need to be node:20-alpine under windows